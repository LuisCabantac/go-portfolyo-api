services:
  go:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - ./.env
    volumes:
      - ./.env:/app/.env:ro
      - rod-browser-cache:/root/.cache/rod

volumes:
  rod-browser-cache:
